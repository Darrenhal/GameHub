<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gamehub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TicTacToe.TicTacToeActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".Snake.SnakeActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".TwentyFortyEight.Twenty48Activity"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".Snake.SnakeGameOver"
            android:parentActivityName=".Snake.SnakeActivity"/>
    </application>

</manifest>